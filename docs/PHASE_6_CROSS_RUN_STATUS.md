# Phase 6 Cross-Run Status

Status: `missing / not evaluated`.

Only one real checkpoint is available per dataset. The required minimum of three independently trained checkpoints under the same protocol is not available, so no cross-run metric was computed and no substitute data was generated.

The following values remain `null`: checkpoint performance screening, graph-edge Jaccard, edge recurrence frequency, weight correlation, intervention impact consistency, and error-direction consistency.

Project decision: defer Phase 6 as a future extension and continue the main single-checkpoint workflow. Current conclusions remain explicitly limited to the supplied checkpoint, data, sample, and graph window.
