import { Section } from './layout/Section';
export function SystemArchitecture(){return <Section id="architecture" eyebrow="Technical architecture" title="Offline replay artifacts, online evidence exploration"><div className="grid gap-4 lg:grid-cols-2"><Layer title="Offline evidence generation" items={['Checkpoint forward pass and graph extraction','Exact relation removal and checkpoint replay','Matched-control generation and statistical analysis','Immutable artifact and run provenance']}/><Layer title="Browser interaction" items={['Artifact loading and structural exploration','Exact selection transfer between workspaces','Intervention and control comparison','Evidence summary and record export']}/></div><div className="mt-4 card p-5"><div className="eyebrow">Model-specific graph adapter</div><div className="mt-4 grid gap-3 md:grid-cols-2"><p className="rounded-lg bg-paper p-3 text-[12px]"><b>DGraFormer</b> → window-level graph context</p><p className="rounded-lg bg-paper p-3 text-[12px]"><b>MSGNet</b> → scale-level graph context</p></div><p className="mt-4 text-[11px] text-ink-400">Both adapters produce the same system object: candidate relation → intervention evidence → matched controls → bounded conclusion. The browser does not rerun a neural model.</p></div></Section>}
function Layer({title,items}:{title:string;items:string[]}){return <div className="card p-5"><h3 className="text-[18px] font-semibold">{title}</h3><ul className="mt-4 space-y-2 text-[12px] text-ink-500">{items.map(x=><li key={x}>• {x}</li>)}</ul></div>}
