import { Section } from './layout/Section';
export function SystemArchitecture(){return <Section id="architecture" eyebrow="Architecture" title="Offline replay, online exploration"><div className="grid gap-4 lg:grid-cols-2"><Layer title="Offline" items={['Extract graphs from checkpoints','Replay relation removal','Generate matched controls','Record provenance']}/><Layer title="In the browser" items={['Explore stored graphs','Transfer the selected relation','Compare intervention and controls','Export evidence']}/></div><div className="mt-4 card p-5"><div className="eyebrow">Graph adapters</div><div className="mt-4 grid gap-3 md:grid-cols-3"><p className="rounded-lg bg-paper p-3 text-[12px]"><b>DGraFormer</b> → window context</p><p className="rounded-lg bg-paper p-3 text-[12px]"><b>MSGNet</b> → scale context</p><p className="rounded-lg bg-paper p-3 text-[12px]"><b>MTGNN</b> → global context</p></div><p className="mt-4 text-[11px] text-ink-400">Each architecture uses a model-specific adapter. The browser reads stored artifacts; it does not rerun the model.</p></div></Section>}
function Layer({title,items}:{title:string;items:string[]}){return <div className="card p-5"><h3 className="text-[18px] font-semibold">{title}</h3><ul className="mt-4 space-y-2 text-[12px] text-ink-500">{items.map(x=><li key={x}>• {x}</li>)}</ul></div>}
