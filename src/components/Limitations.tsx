import { Section } from './layout/Section';
export function Limitations(){return <Section id="limitations" eyebrow="Limitations" title="Evidence is case-specific"><div className="card p-5 text-[12px] leading-relaxed text-ink-500"><p>Results apply only to the named checkpoint, sample, graph context, relation and intervention.</p><p className="mt-3 font-semibold text-ink-700">They describe model behavior, not causal relationships between real-world variables.</p></div></Section>}
