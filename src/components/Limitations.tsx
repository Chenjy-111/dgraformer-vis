import { Section } from './layout/Section';
export function Limitations(){return <Section id="limitations" eyebrow="Limitations" title="A bounded claim about model behavior"><div className="card p-5 text-[12px] leading-relaxed text-ink-500"><p>Evidence is specific to the named model checkpoint, dataset, sample, graph context, relation, and intervention scope. Stored artifacts cover only audited selections; missing evidence is reported as unavailable. Cross-checkpoint stability is not implied.</p><p className="mt-3 font-semibold text-ink-700">This conclusion describes the behavior of the tested forecasting checkpoint under a specified graph intervention. It does not establish a causal relationship between the underlying real-world variables.</p></div></Section>}
