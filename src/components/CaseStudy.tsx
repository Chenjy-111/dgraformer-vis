import { Section } from './layout/Section';
export function CaseStudy(){return <Section id="guided-workflows" eyebrow="Guided workflows" title="The same system task across two graph adapters"><div className="grid gap-4 md:grid-cols-2"><Flow title="DGraFormer workflow" context="Window-level graph context"/><Flow title="MSGNet workflow" context="Scale-level graph context"/></div></Section>}
function Flow({title,context}:{title:string;context:string}){return <div className="card p-5"><h3 className="text-[18px] font-semibold">{title}</h3><p className="mt-2 text-[11px] text-ink-400">Model-specific adapter: {context}</p><p className="mt-5 font-mono text-[12px] text-accent">Discover relation → Test exact relation → Validate evidence</p></div>}
