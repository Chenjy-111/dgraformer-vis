import { Section } from './layout/Section';
const adds=[['Structural discovery','Explore learned graph relations across model-specific temporal contexts.'],['Exact intervention transfer','Carry a selected relation into a stored checkpoint intervention without changing its sample or graph context.'],['Controlled evidence','Compare the response against matched graph interventions and report a bounded conclusion with provenance.']];
export function MethodExplainer(){return <Section id="contributions" eyebrow="What DGraInsight adds" title="A system layer between graph inspection and evidence"><div className="grid gap-4 md:grid-cols-3">{adds.map(([t,b],i)=><article className="card p-5" key={t}><div className="font-mono text-[10px] text-accent">0{i+1}</div><h3 className="mt-2 text-[18px] font-semibold">{t}</h3><p className="mt-3 text-[12px] leading-relaxed text-ink-500">{b}</p></article>)}</div></Section>}
