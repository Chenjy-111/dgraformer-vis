import { Section } from './layout/Section';
const adds=[['Discover','Explore model-specific graph relations.'],['Test','Replay the selected relation in the same context.'],['Validate','Compare its response with matched controls.']];
export function MethodExplainer(){return <Section id="contributions" eyebrow="Method" title="From inspection to evidence"><div className="grid gap-4 md:grid-cols-3">{adds.map(([t,b],i)=><article className="card p-5" key={t}><div className="font-mono text-[10px] text-accent">0{i+1}</div><h3 className="mt-2 text-[18px] font-semibold">{t}</h3><p className="mt-3 text-[12px] leading-relaxed text-ink-500">{b}</p></article>)}</div></Section>}
