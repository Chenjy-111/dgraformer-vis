type ProfileCase={conclusion_id:string;sample_index:number;window_active:boolean;window_exposure_count:number;structural_metrics:{prediction_delta_abs:number;error_delta_mae:number;bh_adjusted_p:number};diagnostic_localization:{variable_ranking:{variable:string;mean_absolute_prediction_delta:number}[];response_intervals:{start_step:number;end_step:number}[]}};

export function CrossSampleProfile({cases,selectedSample,onSelect}:{cases:ProfileCase[];selectedSample:number;onSelect:(sample:number)=>void}){
  const rows=[...cases].sort((a,b)=>a.sample_index-b.sample_index),exposed=rows.filter(c=>c.window_active!==false),measured=exposed.filter(c=>c.structural_metrics.prediction_delta_abs>0).length,up=exposed.filter(c=>c.structural_metrics.error_delta_mae>0).length,down=exposed.filter(c=>c.structural_metrics.error_delta_mae<0).length,supported=exposed.filter(c=>c.structural_metrics.bh_adjusted_p<.05).length;
  return <section className="card overflow-hidden"><header className="border-b border-line bg-[#fafbfd] px-5 py-4"><div className="text-[10px] font-semibold uppercase tracking-[.12em] text-accent">Within-checkpoint sample profile</div><h3 className="mt-1 font-serif text-xl font-semibold text-[#263b59]">Response across audited test periods</h3><p className="mt-1 text-[11px] text-ink-400">Same checkpoint, edge-window and protocol. Unexposed tests are retained but excluded from response summaries.</p></header><div className="p-5"><div className="grid gap-2 sm:grid-cols-4">{[['Exposed / audited',`${exposed.length} / ${rows.length}`],['Measurable responses',measured],['MAE increased / decreased',`${up} / ${down}`],['BH-supported',supported]].map(([label,value])=><div key={label} className="rounded-lg bg-[#f5f7fa] p-3"><div className="text-[9.5px] uppercase tracking-wider text-ink-400">{label}</div><div className="mt-1 text-sm font-semibold text-ink-800">{value}</div></div>)}</div><p className="mt-4 rounded-lg bg-[#f5f7fa] px-3 py-2 text-[10.5px] text-ink-500">No exposed case in this selection obtained BH-adjusted statistical support. Detailed control statistics remain available in stage 5 and the evidence JSON.</p><div className="mt-4 overflow-x-auto"><table className="min-w-[700px] w-full text-left text-[11px]"><thead className="text-ink-400"><tr><th className="p-2">Test</th><th>Exposure</th><th>Prediction 螖</th><th>MAE 螖</th><th>Top output</th><th>Primary interval</th></tr></thead><tbody>{rows.map(c=>{const m=c.structural_metrics,interval=c.diagnostic_localization.response_intervals[0],top=c.diagnostic_localization.variable_ranking[0];return <tr key={c.conclusion_id} onClick={()=>onSelect(c.sample_index)} className={`cursor-pointer border-t border-line hover:bg-[#f3f8f7] ${c.sample_index===selectedSample?'bg-[#edf6f5]':''}`}><td className="p-2 font-semibold">test {c.sample_index}</td>{c.window_active===false?<td colSpan={5} className="font-semibold text-amber-700">Not exposed</td>:<><td>{c.window_exposure_count} input steps</td><td>{m.prediction_delta_abs.toFixed(6)}</td><td>{m.error_delta_mae>=0?'+':''}{m.error_delta_mae.toFixed(6)}</td><td>{top?.mean_absolute_prediction_delta>0?top.variable:'鈥?}</td><td>{interval?`step ${interval.start_step}鈥?{interval.end_step}`:'鈥?}</td></>}</tr>})}</tbody></table></div><p className="mt-3 text-[10px] text-ink-400">Not exposed means the selected graph window was never used by that test input; no zero-valued response or control assessment is shown for that row.</p></div></section>
}
